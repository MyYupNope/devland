import { defineConfig } from 'vite';

export default defineConfig({
    // Set base to the GitHub Pages subdirectory path.
    // If your site is https://myyupnope.github.io/artz/, this must be '/artz/'.
    // If your site is at the root (https://myyupnope.github.io/), change to '/'.
    base: '/artz/',
});
